import { cva } from 'class-variance-authority';

// Archivo de estilos para mantener consistencia con otros componentes screen
// Por ahora vacío, pero puede extenderse en el futuro si se necesitan estilos personalizados

export const languageSelectionStyles = cva('', {
  variants: {},
  defaultVariants: {},
});

