export const onboardingPage = [
  'w-full h-dvh-fallback',
  'flex flex-col',
  'bg-gradient-white-beige font-ui',
  'overflow-hidden',
].join(' ');

export const onboardingShell = [
  'flex-1 min-h-0 flex flex-col items-center justify-start',
  'overflow-hidden',
  'px-[clamp(0.75rem,3vw,3rem)]',
  'py-[clamp(0.75rem,2.5vw,2.5rem)]',
  'gap-[clamp(1.25rem,4vw,2.75rem)]',
  'short-landscape:px-[clamp(0.5rem,2vw,1.25rem)]',
  'short-landscape:py-[clamp(0.5rem,1.5vw,1.25rem)]',
  'short-landscape:gap-[clamp(0.75rem,2vw,1.5rem)]',
].join(' ');

export const onboardingLogoWrapper = [
  'flex items-center justify-center shrink-0',
  'pb-[clamp(0.75rem,2vw,1.5rem)]',
  'short-landscape:pb-[clamp(0.35rem,1vw,0.75rem)]',
].join(' ');

export const onboardingCard = [
  'flex flex-1 min-h-0 w-full flex-col overflow-hidden',
  'rounded-[28px] bg-white km0-card-shadow mx-auto',
  'max-w-[clamp(320px,92vw,1120px)]',
  'px-[clamp(1.5rem,4vw,3.75rem)]',
  'py-[clamp(1.5rem,4vw,4rem)]',
  'gap-[clamp(1.25rem,4vw,2.75rem)]',
  'short-landscape:flex-row short-landscape:items-stretch',
  'short-landscape:gap-[clamp(1rem,3vw,2rem)]',
  'short-landscape:p-[clamp(1rem,2vw,1.5rem)]',
  'short-landscape:max-h-[calc(100dvh-72px)]',
].join(' ');

export const onboardingViewportWrapper = 'flex-1 min-h-0 w-full';

export const onboardingFooter = [
  'flex w-full items-center justify-between',
  'border-t border-neutral-100',
  'pt-[clamp(0.75rem,2vw,1.5rem)]',
  'gap-[clamp(0.5rem,2vw,1.5rem)]',
  'short-landscape:pt-[clamp(0.5rem,1vw,1rem)]',
].join(' ');

export const onboardingCounter = [
  'text-km0-blue-700 font-bold shrink-0 text-center',
  'text-[clamp(0.9rem,2vw,1.1rem)]',
  'w-[clamp(2.5rem,4vw,3rem)]',
].join(' ');

